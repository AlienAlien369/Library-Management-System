# Copy to .env.local for local development (CRA reads .env.local automatically).
# On Vercel, set this as a real Project Environment Variable instead.
REACT_APP_API_URL=https://your-api.onrender.com/api/
